{{- if eq .Values.ecto.baseKey.hardcoded true -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ecto.baseKey.secret.name }}
type: Opaque
data:
  secret_key: {{ .Values.ecto.baseKey.value }}
immutable: true
{{- end -}}
